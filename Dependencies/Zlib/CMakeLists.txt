project(ZLIB)
add_library(ZLIB STATIC IMPORTED GLOBAL)

set_target_properties(ZLIB PROPERTIES
		IMPORTED_IMPLIB ${PROJECT_SOURCE_DIR}/${LIB_DIR}/zlib.lib
		INTERFACE_INCLUDE_DIRECTORIES ${PROJECT_SOURCE_DIR}/include
		)
